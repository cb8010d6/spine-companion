{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Allow the main companion window to use Tauri core IPC APIs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
